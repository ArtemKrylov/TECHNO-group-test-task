import { Container } from 'components/App/App.styled';
import styled from 'styled-components';
const StartPageStyled = styled(Container)`
  section {
    display: flex;
    flex-direction: column;
    gap: 25px;
    align-items: start;
  }
`;
export default StartPageStyled;
